# oracle/ (deprecated for production)

Production runs on Vercel Cron — see `frontend/app/api/cron/*` and `frontend/lib/oracle/*`. This folder is kept for local development and reference only; do not deploy it.
